# State — где мы сейчас

Живой файл: статус + следующий шаг. Обновлять в КОНЦЕ каждой сессии.
Последнее обновление: **2026-06-26**.

## Фаза
Рефакторинг завершён. **HTTPS-этап завершён**: бэкенд работает за Caddy с сертификатом.
Идём к прод-релизу в Chrome Web Store (заменяем старую PDFCrowd-версию).

## Сделано в этой сессии
- 🟢 **Полоски в селективном экспорте — закрыто** (фикс в `blockmode.js`, протестировано).
- 🟢 **HTTPS для бэкенда поднят целиком:**
  - Домен `export-gpt.duckdns.org` (DuckDNS) → A-запись на `89.167.13.19`.
  - На VPS поставлен **Caddy** (reverse proxy перед Gotenberg), авто-сертификат Let's Encrypt (продление автоматом).
  - Hetzner-фаервол: открыты `80/443`, **закрыт внешний `3000`** (Caddy ходит к Gotenberg по localhost).
  - Доступ к серверу: сброшен root-пароль, добавлен SSH-ключ Мака (вход без пароля).
- 🟡 **Расширение переведено на новый адрес (НЕ закоммичено):**
  - `background.js`: `GOTENBERG_URL` → `https://export-gpt.duckdns.org/forms/chromium/convert/html`.
  - `manifest.json`: host_permission → `https://export-gpt.duckdns.org/*` (убран http-IP).
  - Локально проверено: экспорт работает, health-чек отдаёт `200` по https.

## Следующие шаги (по порядку)
1. **Коммит** правки (`background.js` + `manifest.json`) — делает Igor. Суть сообщения: «Switch PDF backend to HTTPS domain instead of raw-IP HTTP».
2. **Публикация** новой версии в CWS (отдельный ручной процесс, идёт на модерацию). Учесть: смена `host_permissions` может попросить пользователей переподтвердить расширение при обновлении.
3. **Остаток Stage 5:**
   - Поднять на VPS `--api-timeout` / ресурсы Gotenberg (лечит 503/500 на больших чатах).
   - Удалить мёртвые консты `minImageDuration`, `buttonIconFill` (`common.js`).
   - Чистая сборка CWS-пакета (только файлы расширения).


## Архитектура сервера (прод)
Клиент → `https`(443) → Hetzner-фаервол → **Caddy** (TLS-терминация, сертификат) → `reverse_proxy localhost:3000` → **Gotenberg** (Docker) → PDF → Caddy шифрует → клиент.
- Конфиг Caddy: `/etc/caddy/Caddyfile`; служба `caddy` (systemd). Вход: `ssh root@89.167.13.19` (по ключу).
- Опубликованная версия в сторе — на **PDFCrowd**, не на нашем VPS; CWS сам обновит людей на новую версию.

## Мелкие известные issue (приемлемо)
- Wikipedia-стиль inline link — `href` не резолвится.
- 500/503 на тяжёлых/фоновых экспортах — серверное, лечится ресурсами/таймаутом Gotenberg (шаг 3).
