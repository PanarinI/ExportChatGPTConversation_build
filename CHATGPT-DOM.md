# Карта DOM ChatGPT — что мы про него ЗАМЕРИЛИ

Орган. Растёт. Здесь лежит только то, что проверено на живой странице: чем страница является,
а не чем она кажется. Каждый пункт — дата замера, что именно измерено, что из-за этого ломалось,
как живём. Догадки помечены 🔶 и не считаются знанием.

**Зачем отдельный дом.** Общее знание про «веб-страницы» здесь бесполезно: у ChatGPT почти каждый
видимый признак локален для окна и протухает от их релиза. Три раза подряд поломка приходила не из
нашего кода, а из умершего допущения о ЧУЖОМ DOM — см. столбец «что сломалось».

**Правило пользования.** Меняешь харвест — сперва читай эту таблицу. Допущение старше полугода
считается протухшим, пока не перемерено. Новый замер дописывается СЮДА, «почему так решили» — в
`DECISIONS.md`.

---

## 1. Лента виртуализирована

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| до 07-2026 | турны вне экрана оставались УЗЛАМИ с пустым `innerHTML` | — | признак «есть пустые заглушки» = «есть что дорисовать» |
| ✅ 2026-07-20 | турны вне экрана **удаляются** из DOM. В 94-турном чате смонтировано `16·18·31·37·83…94`, заглушек 0 | признак молча ответил «всё отрисовано» → харвест не прокручивал ни пикселя, в PDF уезжало смонтированное окно (~15 турнов) | решение по ПРОКРУЧИВАЕМОСТИ: лента выше вьюпорта → DOM не может держать её всю. Плюс пересборка диалога вставкой, а не замещением |

## 2. `scrollTop = 0` — это не начало диалога

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| ✅ 2026-08-26 | ноль = верх **загруженного окна**. Старое подтягивается по мере подъёма; в свежей вкладке лежит ~40 турнов | харвест шёл только вниз → начинал с середины по построению. Два экспорта подряд: 40–60 страниц, ни один не с начала | фаза подъёма ПЕРЕД спуском: шаг вверх 0.7 экрана с захватом по дороге, на нуле — ожидание |
| ✅ 2026-08-27 | стояние у верха 212 с с хуком на `fetch`/XHR: **0 сетевых запросов**, турнов как было 202 | «терпение» лечило не ту болезнь | ждать наверху имеет смысл, только если ожидание уже раз окупилось (терпение зарабатывается) |

## 3. Номер турна считает ОКНО и сдвигается

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| 🔶 2026-07-20 | в 94-турном чате номера `83…94` — выглядело сквозным по диалогу | — | (допущение, прожило месяц) |
| ✅ 2026-08-26 | три экспорта одной вкладки: `1→40`, `1→70`, `1→190`. Низ — всегда самое свежее сообщение, значит растущий номер считает загруженное окно. При подгрузке диалог **перенумеровывается целиком** | кэш был ключёван `data-testid` → один ключ означал разные сообщения; выход «дошли до турна №1» врал | личность турна = **`data-message-id`** (id сервера, есть в DOM — проверено). `data-testid` только запасной путь |

## 4. Дыры в нумерации не видят потерянного НАЧАЛА

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| ✅ 2026-08-26 | `missingTurnIndices` ищет дыры МЕЖДУ пойманными номерами; отсутствующий хвост слева — не дыра, а сдвинутая граница | диалог с потерянной половиной честно рапортовал `still missing: 0` | полнота считается наблюдаемо: сколько поймали против того, сколько страница держит (максимальный номер в DOM) |

## 5. Лента дёргается вниз, когда трогаешь верх

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| ✅ 2026-08-27 | автор глазами: полоса прокрутки к верху → резко вниз → снова вверх, много раз, ~10 секунд. Чат из 4 сообщений стоил 20 секунд | 1) «нас сдвинуло вниз» читалось как «приехал контент» → цикл бился; 2) прыжок давал снимок без перекрытия → его турны дописывались В КОНЕЦ, и документ выходил **повёрнутым** | 1) прогресс = список ВЫРОС, а не «нас сдвинуло»; 2) порядок = координата, а не последовательность (см. ниже) |

## 6. Порядок: координата вместо цепочки

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| ✅ 2026-08-27 | три PDF одного чата по 81 странице: текст одинаковый (266 651 против 266 675 знаков), но кусок с 10 % одного файла лежит на 91 % другого; два из трёх открываются на 25 янв. вместо 18 янв. | порядок собирался склейкой снимков по перекрытию — допущение «снимки идут сплошным проходом». Прыжок ломал цепочку | каждый снимок привязывается к любому известному турну (медиана дельт номеров) → все турны в ОДНОЙ системе координат. Перенумерация стала измеряемой величиной, а не помехой |

## 7. Картинки

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| ✅ 2026-06-26 | DALL-E-картинка — `position:absolute` внутри aspect-box | в PDF коробка схлопывается | `extractDalleImages` вытаскивает `<img>` в нормальный поток (общая для обоих путей экспорта) |
| ✅ 2026-06-26 | экспорт мутирует живой `<img>` в base64 → детектор по URL переставал её узнавать | после первого экспорта пропадал чекбокс в блок-режиме | опознание по трём признакам: контейнер `imagegen` + URL + base64 |
| ✅ 2026-08-06 | загруженная юзером картинка обёрнута в labeled-`<button>` | `cleanupForPdf` сносил кнопку вместе с картинкой | keep-путь для user-uploaded (repro от юзера Bela → 1★ переписан в 4★) |
| ✅ 2026-08-22 | картинки под авторизацией (`oaiusercontent.com`) тянутся фоном, `readAsDataURL` сохраняет исходный **webp/jpeg**; гейт `extractDalleImages` открывался только на `data:image/png` длиннее 500 000 символов | в поздних турнах обёртка не вскрывалась → пустая коробка | 🔶 открытый хвост: решать по структуре обёртки, а не по весу строки |
| ✅ 2026-08-22 | у каждого фонового фетча потолок 5 с без повтора; по таймауту `src` остаётся удалённым, а в PDF рисуется иконка «битая картинка» **14×16** | «бывает что картинки не выгружаются» | 🔶 лечение известно: поднять потолок, ограничить одновременность, один повтор |

## 8. Прочее, замеренное

| дата | замер | что сломалось | как живём |
|---|---|---|---|
| ✅ 2026-07-20 | ссылка чата ушла из `<nav>`; заголовок вкладки ОТСТАЁТ (свежепереименованный чат ещё «New chat») | два разных чата давали одинаковые имена файлов | три ступени: ссылка по всему документу → заголовок → первый вопрос; generic-имена отвергаются |
| ✅ 2026-07-20 | `href` у ссылок-цитат дозагружаются асинхронно | ранний снимок = синие некликабельные ссылки | снимки оцениваются по числу резолвнутых ссылок, турны с неразрешёнными довизитятся |
| 🔶 2026-07-2X | вкладка в фоне → Chrome душит таймеры, харвест рвётся (тяжелее на DALL-E) | «первые 4 и последние 4, середина выпала» | строка в спиннере «держите вкладку впереди»; пауза по `document.hidden` не сделана |
| ✅ 2026-08-27 | в **headless**-Chrome события `scroll` почти не доставляются: 2 события на весь прогон (они привязаны к кадрам) | стенд на скролл молча ничего не проверял | сцены стендов опрашивают позицию таймером, а не слушают `scroll` |

---

## Что из этого следует для чтения чужого DOM

1. **Любой видимый счётчик считает окно, пока не доказано обратное.** Номер турна, длина списка,
   «первый элемент» — всё это про загруженное, а не про сущее.
2. **Признак полноты обязан уметь назвать свой слепой угол.** «Дыр нет» ≠ «всё на месте», если
   признак не смотрит за границу.
3. **Относительный порядок DOM не врёт — врут номера.** Строить на порядке, номера использовать
   как измеряемую величину со сдвигом.
4. **Допущение о чужом DOM протухает молча.** Оно не падает с ошибкой, оно начинает тихо врать.
   Поэтому дата замера здесь важнее формулировки.
